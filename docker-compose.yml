version: "3"
# this file uses prebuilt image in dockerhub
services:
  jekyll:
    image: amirpourmand/al-folio
    command: ["/bin/bash", "-lc", "(bundle check || bundle install) && exec /tmp/entry_point.sh"]
    ports:
      - 8080:8080
    volumes:
      - .:/srv/jekyll
